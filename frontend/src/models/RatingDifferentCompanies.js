export default class RatingDifferentCompanies {
  constructor() {
    (this.id = null),
      (this.service = null),
      (this.specific = null),
      (this.returnFlightRating = null),
      (this.returnCompanyRating = null);
  }
}
