export default class Destination {
  constructor() {
    (this.name = null),
      (this.country = null),
      (this.zip = null),
      (this.airport = null);
  }
  reset() {
    (this.name = null),
      (this.country = null),
      (this.zip = null),
      (this.airport = null);
  }
}
