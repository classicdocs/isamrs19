export default class HotelsOffer {
  constructor() {
    (this.id = null),
      (this.price = null),
      (this.type = null),
      (this.description = null);
  }

  reset() {
    (this.id = null),
      (this.price = null),
      (this.type = null),
      (this.description = null);
  }
}
