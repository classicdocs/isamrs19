export default class Airplane {
  constructor() {
    (this.model = null),
      (this.seatsFirstRows = null),
      (this.seatsFirstCols = null),
      (this.seatsBusinessRows = null),
      (this.seatsBusinessCols = null),
      (this.seatsEconomyRows = null),
      (this.seatsEconomyCols = null);
  }
  reset() {
    (this.model = null),
      (this.seatsFirstRows = null),
      (this.seatsFirstCols = null),
      (this.seatsBusinessRows = null),
      (this.seatsBusinessCols = null),
      (this.seatsEconomyRows = null),
      (this.seatsEconomyCols = null);
  }
}
