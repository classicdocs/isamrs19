export default class RoomDiscount {
  constructor() {
    (this.id = null),
      (this.startDate = null),
      (this.endDate = null),
      (this.price = null),
      (this.additionalServices = []);
  }
}
