const ENVIRONMENTS = {
  'localhost': 'dev'
};

const BACKEND_HOSTNAMES = {
  'dev': 'http://localhost:8088'
};

const API_ROUTE = '/api';

export {
  ENVIRONMENTS,
  BACKEND_HOSTNAMES,
  API_ROUTE
};